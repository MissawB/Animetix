I am an implementer subagent performing Task 1 of the requests-to-httpx migration.

Goal: Migrate requests to httpx (sync) in:
- backend/adapters/inference/brain_api_adapter.py
- backend/adapters/inference/local_rerank_adapter.py
- backend/adapters/inference/unified_inference_adapter.py

Guidelines:
1. Replace import requests with import httpx.
2. Replace requests.post(url, ...) with httpx.post(url, ..., follow_redirects=True).
3. Replace requests.get(url, ...) with httpx.get(url, ..., follow_redirects=True).
4. Replace requests.exceptions.RequestException with httpx.RequestError.
5. In UnifiedInferenceAdapter.stream_generate, migrate requests.post(..., stream=True) to use httpx.stream(\"POST\", url, ..., follow_redirects=True).

Example for stream_generate:
```python
with httpx.stream(\"POST\", url, json=payload, headers=self._get_headers(), follow_redirects=True, timeout=self.timeout) as res:
    res.raise_for_status()
    for line in res.iter_lines():
        if line:
            # ...
```

I will now apply these changes and commit them.
